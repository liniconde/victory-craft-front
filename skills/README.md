Project-local skills source lives here.

- `skills/frontend-design/SKILL.md`: source of the custom `frontend-design` skill.

To make Codex load a project skill automatically, install or symlink it into `~/.codex/skills/<skill-name>/SKILL.md`.
